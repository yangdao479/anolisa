# PDF Extract

Extract text content from PDF documents.

This skill reads PDF files and extracts their textual content,
preserving basic structure like headings and paragraphs.

## Parameters

- `path` (string, required): Path to the PDF file
- `pages` (string, optional): Page range to extract (e.g., "1-5")
