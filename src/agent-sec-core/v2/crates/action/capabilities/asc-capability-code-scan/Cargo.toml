[package]
name = "asc-capability-code-scan"
description = "Pre-execution regex security scanner for Bash and Python code"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true
repository.workspace = true
publish.workspace = true

[dependencies]
asc-action-runtime = { workspace = true }
asc-action-types = { workspace = true }
fancy-regex = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
yaml-rust2 = { workspace = true }

[dev-dependencies]
# The shipped-rule test compiles every pattern with the same engine the scanner
# uses, so it needs the dependency in its own right.
fancy-regex = { workspace = true }
# The scanner test asserts the serialized JSON contract byte for byte.
serde_json = { workspace = true }

[lints]
workspace = true
