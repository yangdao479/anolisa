// Aggregates all former standalone integration tests as modules.
mod bwrap_seccomp;
mod demo_coverage_high;
// For low-coverage scenario, replace the line above with:
// mod demo_coverage_low;
mod managed_proxy;
